[project]
name = "flowchart-graph-benchmark"
version = "0.1.0"
description = "Flowchart crop to graph JSON / Mermaid benchmark"
readme = "README.md"
requires-python = ">=3.11,<3.13"

[tool.uv]
package = false
